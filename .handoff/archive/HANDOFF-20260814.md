# Handoff: OfferPath

**Date:** 2026-08-14
**Agent:** Codex
**Branch:** main
**Latest commit:** 40257e6 "fix(supabase): accept sb_publishable_ key format in checkIsConfigured"
**Remote:** https://github.com/alexjiaguo/offerpath.git
**Vercel:** https://offerpath.cc.cd (production, deployed from main at 40257e6, Aug 10)

## Session Summary

### Branch cleanup (this session)

Reviewed all branches, compared against Vercel production, and cleaned up stale branches:

- **Deleted `codex/resume-studio-flowcv`** (e687d77) - fully synced to origin, safe delete
- **Deleted `codex/resume-studio-resumecom`** (629c153) - fully synced to origin, safe delete
- **Pruned 3 stale worktrees** (resume-flowcv, resume-resumecom, resume-resumeio) - directories already removed from disk, git metadata pruned
- **Deleted `codex/login-redesign`** (0ab7d04) - was a direct ancestor of main (0 unique commits, 9 behind). Force-deleted with `-D` because `-d` refused due to stale remote tracking branch being behind local

### Prior session work (uncommitted, from Antigravity session Aug 8-12)

The uncommitted changes on main represent in-progress resume parser work:

1. **OpenResume parser port** (`src/lib/open-resume-parser/`) - Local PDF parser ported from OpenResume (MIT license). No API key required. Uses feature-scoring pipeline: PDF text items -> lines -> sections -> structured ResumeData. 5 files: index.ts, extractors.ts, helpers.ts, pdf-reader.ts, types.ts
2. **ResumeParserService rewrite** (+1235/-~500 lines) - Major overhaul integrating the OpenResume parser approach
3. **FileParserService changes** (+303 lines) - Supporting new parser pipeline
4. **aiService.ts** (+202 lines) - Added `parseResumeWithAI()`: LLM-first parsing with regex/ResumeParserService fallback
5. **New test file** `src/tests/lib/ResumeParserService.test.ts` - Tests for the rewritten parser
6. **Supabase local dev setup** - `supabase/config.toml` (full local Supabase config) + `supabase/.gitignore`
7. **Supabase MCP server** added to `.codex/config.toml` + `.gitignore` entry to prevent committing the PAT
8. **AGENTS.md typechange** - was a symlink (mode 120000), now a regular file with expanded RuFlo V3 config (+244 lines)

## Git State (Verified 2026-08-14)

- **Branch:** main
- **HEAD:** 40257e6 (synced with origin/main and Vercel production)
- **Uncommitted:** 8 modified files + 4 untracked items (see below)
- **Stash:** 1 (`stash@{0}: On main: main branch uncommitted changes (favicon.svg, logo-mark.svg deletion)`)
- **Cannot commit:** `.git` is read-only in sandbox environment

### Modified files

| File | Changes |
|------|---------|
| `.codex/config.toml` | Added Supabase MCP server config (contains Supabase PAT - see Security) |
| `.gitignore` | Added `.codex/config.toml` to prevent committing PAT |
| `AGENTS.md` | Typechange: symlink -> regular file, +244 lines (RuFlo V3 config) |
| `src/app/dashboard/resume/new/page.tsx` | Modified (+69 lines) |
| `src/lib/FileParserService.ts` | Major rewrite (+303 lines) - new parser pipeline |
| `src/lib/ResumeParserService.ts` | Major rewrite (+1235/-~500 lines) - OpenResume integration |
| `src/lib/aiService.ts` | +202 lines - `parseResumeWithAI()` with LLM + regex fallback |
| `src/tests/lib/aiService.test.ts` | +207 lines - new tests |

### Untracked files

| Path | Purpose |
|------|---------|
| `src/lib/open-resume-parser/` | OpenResume PDF parser port (5 files, MIT license) |
| `src/tests/lib/ResumeParserService.test.ts` | Tests for rewritten parser |
| `supabase/.gitignore` | Supabase local dev gitignore |
| `supabase/config.toml` | Supabase local development config |

## Remaining Branches

| Branch | Commit | Status |
|--------|--------|--------|
| `main` (current) | 40257e6 | Synced with origin/main and Vercel |
| `codex/new-resume-studio` | 5ea9530 | 3 commits ahead of main, local only (not pushed) |

### Remote branches with no local counterpart (local deleted this session)

- `origin/codex/login-redesign`
- `origin/codex/resume-studio-flowcv`
- `origin/codex/resume-studio-resumecom`
- `origin/codex/resume-studio-resumeio`
- `origin/redesign/ui`

These can be deleted on GitHub with `git push origin --delete <branch>` if no longer needed.

## Running Processes

- **node on localhost:19825** - purpose unknown, likely a lingering process
- **node on *:3210** - likely a dev server

Kill with `lsof -ti :19825,:3210 | xargs kill` if they interfere.

## Build & Test

- **Build:** `rm -rf .next && npm run build`
- **Tests:** `npm test`
- **Dev server:** `next dev` (may fail due to macOS FSEvents limit - use production build + standalone server as fallback)
- **Standalone server:** `cd .next/standalone && PORT=<port> HOSTNAME=localhost node server.js`

## Security

- `.codex/config.toml` contains a **Supabase access token** (`sbp_...`). The `.gitignore` change adds this file to ignore, but it is **already tracked** in git. To fully untrack: `git rm --cached .codex/config.toml` (requires `.git` write access).
- Do not commit `.codex/config.toml` in its current state. The PAT should be rotated if it was ever pushed to the remote.

## Dead Ends & Ruled-Out Approaches

1. **`next dev` (Turbopack) unreliable** - macOS FSEvents limit exhausted by stuck node processes. Use production build + standalone server instead.
2. **`.git` is read-only in sandbox** - Cannot commit, stash, or prune worktrees without escalated permissions.
3. **`ps` command not permitted** in sandbox - Use `lsof` instead to find processes.
4. **`git branch -d` refuses merged branches** when the remote tracking branch is behind local. Use `git branch -D` when the branch is confirmed merged into HEAD.
5. **Previous worktrees were discarded** - User confirmed worktree changes (resume-flowcv, resume-resumecom, resume-resumeio) are discardable. The `worktrees/` directory was removed in a prior session.
6. **OpenResume parser has debug console.log statements** in `index.ts` - these should be removed or gated behind a debug flag before production.

## Decisions Made

1. **Deleted 3 stale branches** - All were either fully synced to remotes or fully merged into main. No work lost.
2. **Vercel production confirmed as main** - Verified via `vercel inspect`: latest production deployment built from `main` at `40257e6` (Aug 10). Alias `offerpath-git-main-alexjiaguos-projects.vercel.app`.
3. **`codex/new-resume-studio` kept** - Has 3 unique commits not in main. Not deleted because it contains unmerged work.
4. **Remote branches not deleted** - Only local branches were deleted. Remote branches remain on GitHub for the user to clean up if desired.

## Do-Not-Touch Zones

1. **Template hex colors** - Hardcoded in templates for PDF export. Do not tokenize.
2. **`.agents/` directory** - Agent artifacts from previous sessions. Leave as-is.
3. **`.git`** - Read-only in sandbox. Use escalated permissions for git write operations.
4. **`AGENTS.md`** - Now a regular file (was symlink). Contains RuFlo V3 project config.
5. **`src/lib/open-resume-parser/`** - MIT-licensed port from OpenResume. Preserve license attribution.

## Next Steps

1. **Commit the resume parser work** - 8 modified + 4 untracked files represent a cohesive feature: local PDF parsing via OpenResume port + LLM-first parsing with regex fallback. Suggested commit message: `feat(resume): add OpenResume local PDF parser with AI fallback`.
2. **Untrack `.codex/config.toml`** - Run `git rm --cached .codex/config.toml` to stop tracking the file with the Supabase PAT. Consider rotating the PAT.
3. **Clean up remote branches** - 5 remote branches have no local counterpart. Delete with `git push origin --delete <branch>` if no longer needed.
4. **Test the parser end-to-end** - Upload a real PDF resume and verify extraction quality. Remove debug `console.log` statements in `open-resume-parser/index.ts`.
5. **Decide on `codex/new-resume-studio`** - 3 commits ahead of main, not pushed. Either merge into main or rebase and push.
6. **Run Supabase locally** - `supabase/config.toml` is set up. Run `supabase start` to test local development.

## Environment

- **Node:** 25.6.0
- **Vercel CLI:** 54.14.0
- **Vercel project:** `prj_EZQ6b6K1Ato7Pmb2qQQZUodJdpLl` (team: `alexjiaguos-projects`)
- **Supabase project ref:** `ubkywtxwzrudstqvpafh`
- **AI Skills Hub:** `/Volumes/Download/ai-skills-hub/`
- **Skills sync:** `bash /Volumes/Download/ai-skills-hub/sync_skills.sh ~/.codex/skills`
