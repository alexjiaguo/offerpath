# Agent Handoff Document

> This file is the bridge between agents working on the same project.
> The outgoing agent fills it in. The incoming agent reads it first.
> Location: `.handoff/HANDOFF.md` in the project root.
> If a section is genuinely empty, write "none" - do not delete it.

## Metadata

- **Project**: OfferPath
- **Project path**: `/Volumes/Download/ai-projects/side-hustles/job-hunt-os/products/offerpath`
- **From agent**: Cursor
- **To agent**: any
- **Date**: 2026-08-15 14:55 GMT+8
- **Session summary**: Dual-mode Form/Studio resume editor, Teal/FlowCV copy pass, markdown `**` unwrap, and a webpack-cache recovery after `/dashboard/resume` 500s.

## Git State (verified against actual `git status`)

- **Branch**: `main` (confirmed via `git branch --show-current`)
- **HEAD**: `40257e6` — `fix(supabase): accept sb_publishable_ key format in checkIsConfigured`
- **Remote**: `origin/main` — **up to date** (`## main...origin/main`)
- **Uncommitted changes**: **yes — leave working tree as-is**. Mixed staged + unstaged + untracked from multiple sessions (parser, dual-mode studio, copy audit). Do **not** `git checkout` / `git reset` / stash without reading this file. Not committed: user did not ask to commit, and the tree is not one cohesive reviewable commit.
- **Stashes**: `stash@{0}: On main: main branch uncommitted changes (favicon.svg, logo-mark.svg deletion)`
- **Recent commits**:
  ```
  40257e6 fix(supabase): accept sb_publishable_ key format in checkIsConfigured
  7c94a0c fix(auth): clean slate on signup/login and ensure Supabase profile row
  d772d55 feat(db): sync database schema with code, wire up all 7 tables, remove connection blacklist
  21f7f09 fix: resolve lint warnings for unused variables and missing effect dependencies
  c5ae104 feat: refine resume editor UI and fix hydration error
  ```

### Staged (index) vs unstaged

Partial index from an earlier studio port (then more edits on top — many files are `AM` / `MM`):

- **Staged new**: `src/components/resume/editable/{EditContext,EditableText,FormatToolbar,InlineControls,StylePanel,fieldPath}.*`, `src/tests/editableFieldPath.test.ts`, plus staged hunks of all 9 templates + `shared.ts`
- **Unstaged**: large overlay — dashboard/landing/auth, stores, parser, copy, chrome, ThemePicker, etc.
- **Untracked (must keep)**: `ClickStudio.tsx`, `EditorChrome.tsx`, `FormStudio.tsx`, `editable/EditableDateRange.tsx`, `src/lib/markdownInline.ts`, `src/lib/editorSplit.ts`, `src/lib/open-resume-parser/`, `src/lib/resumeUploadPipeline.ts`, `src/lib/pdfWorker.ts`, `public/pdf.worker.min.mjs`, privacy/terms/not-found, supabase local config, several tests

`AGENTS.md` shows `T` (symlink → regular file). `.codex/config.toml` is still **tracked** even though `.gitignore` lists it (contains a Supabase PAT).

### Verified vs. Assumed

- **Verified working**: `npm test` → 15 files, **124 passed** (after `markdownInline.ts` extract). `npm run build` succeeded earlier the same day (Next.js 15.5.15). After cache wipe, `GET /dashboard/resume` and `GET /` returned 200 from `next-server` on :3000.
- **Assumed / unverified**: no full `npm run build` after extracting `src/lib/markdownInline.ts`. No live click-through of Form↔Studio after the copy pass. Cursor Simple Browser `ERR_CONNECTION_REFUSED` while curl returned 200 — treated as a stale tab / `[::1]` quirk, not a current crash.

## What Was Done

### This Cursor session (2026-08-15)

- **Dual-mode editor** (Form + Studio, both kept):
  - Shell: `src/app/dashboard/resume/[id]/page.tsx` (**499 lines — at the 500-line cap**)
  - Form: `src/components/resume/FormStudio.tsx` — contain-scale preview (`AutoScaledPreview` default `fit="contain"`)
  - Studio: `src/components/resume/ClickStudio.tsx` — unscaled `w-[210mm]` paper; **never** put `contentEditable` under `transform: scale()`
  - Shared left-pane top chrome: `src/components/resume/EditorChrome.tsx` (Tailor → template + Design → validation/fit/Save/export). Used by FormStudio and StylePanel
  - Per-resume `editorMode?: "form" | "studio"` on `Resume`; persist locally; strip before `saveResumeAction`
  - Click-to-edit from `codex/new-resume-studio` `5ea9530`: `editable/*`, `fieldPath.ts`, `EditableDateRange` (start/end not bound to `formatDates()`)
- **Project names**: `src/lib/markdownInline.ts` (`unwrapMarkdownBold`, `markdownInlineToHtml`). Plain name fields strip `**Name**`; HTML fields convert `**` → `<strong>`. Form project input unwraps **on blur**. `markdownConverter.ts` unwraps on parse/serialize. AI tailor/parse prompts: plain strings, no markdown asterisks.
- **Theme popup**: `ThemePicker.tsx` — button label **Design**, `h-7 w-7` chips, preview fills right column (`fit="width"`). `data-live-preview` only on **contain** live preview so the modal does not steal `PageFitIndicator` measure. Badge: Fits / Over / Under 1 page (overflow if `> 100`).
- **Copy**: sci-fi UI strings replaced (Career Asset Studio → Resumes, All Leads → All jobs, Visual DNA → Design, Story Bank → STAR stories, nav Resume Studio → Resumes, etc.). Keep `is_base`, `editorMode`, routes, Zustand keys, OfferPath, Form/Studio labels.
- **Runtime**: Fast Refresh poisoned `.next` (`TypeError: Cannot read properties of undefined (reading 'call')` then ENOENT `@phosphor-icons.js`). Fix: stop `next`, `rm -rf .next`, `npm run dev`. `markdownConverter` must **not** import `'use client'` `templates/shared.ts` — use `@/lib/markdownInline`.
- **Handoff freeze**: OfferPath `next` on :3000 **stopped**. Prior Codex handoff archived to `.handoff/archive/HANDOFF-20260814.md`.

### Still in the working tree from earlier sessions (not this chat)

- OpenResume local PDF parser: `src/lib/open-resume-parser/`, `ResumeParserService`, `FileParserService`, `parseResumeWithAI()`
- Landing/auth/privacy/terms, stores, supabase local `supabase/config.toml`
- See archive `.handoff/archive/HANDOFF-20260814.md`

## In Progress

- Entire `main` working tree uncommitted (parser + studio + copy + landing).
- Cursor browser still may show `ERR_CONNECTION_REFUSED` for `http://localhost:3000/` after the server was stopped for this freeze.
- `codex/new-resume-studio` at `5ea9530` (3 commits not in main, local only) — studio UI was **ported into the uncommitted working tree**, not merged as a git merge.

## Dead Ends & Ruled-Out Approaches

- **Replace Form with Studio / Content-Design tabs**: user constraint — dual-mode stays; labels stay **Form** / **Studio**.
- **`html={true}` on project names**: XSS / format-toolbar noise. Unwrap + CSS `fontWeight: 700` instead.
- **ThemePicker preview wrapping the live studio canvas in `transform: scale()`**: breaks contentEditable. Preview = read-only `ResumePreview` + `AutoScaledPreview fit="width"`.
- **Fixing ThemePicker layout by restyling the live studio paper**: wrong tree; popup preview is separate.
- **Putting ThemePicker in both StylePanel and EditorChrome**: one popup only (chrome).
- **Calling 101–105% fill “1 page”**: lie; threshold is now `> 100` for overflow; green state is “Fits 1 page” without a misleading percent.
- **Importing `unwrapMarkdownBold` from `templates/shared.ts` into `markdownConverter.ts`**: `shared.ts` is `'use client'` + DOMPurify; webpack can emit `undefined.call`. Helpers live in `src/lib/markdownInline.ts`.
- **Treating Cursor Simple Browser `ERR_CONNECTION_REFUSED` as a dead server** while `lsof` shows `next-server` on :3000 and curl 200: stale tab / `localhost`→`[::1]`. Next logged: cross-origin from `[::1]` to `/_next/*`. Try `http://127.0.0.1:3000/`.
- **`git branch -d` on merged locals** when remote-tracking is behind: needs `-D` (prior session).
- **Do not “fix” DOCX by adding projects** unless asked — `exportDocx.ts` currently omits projects.

## Do Not Touch

- `src/app/dashboard/resume/[id]/page.tsx`: already **499 lines**. Extract (e.g. fullscreen overlay) before adding UI.
- `is_base`, `editorMode`, routes (`/dashboard/pipeline`, `/dashboard/resume`, …), Zustand persist keys, `getUniqueArchetypes` (UI label is “Role type” only).
- Template **ids** (e.g. `premium-headshot`) and theme preset **ids** (`corporate-navy`, …). Display names may change.
- Template hardcoded hex colors (PDF export).
- `~/.cursor/plans` (user constraint).
- `.codex/config.toml`: tracked file with **Supabase PAT**. Do not commit. Untrack with `git rm --cached .codex/config.toml`.
- `src/lib/open-resume-parser/`: MIT OpenResume port; keep attribution.
- `AGENTS.md`: typechange symlink→file from a prior session; do not “fix” the symlink back.
- `.agents/`: prior agent artifacts.
- nba_chat Next on other ports (`next-server` v16.3.0) — not this repo.

## Next Steps

1. **Thaw**: `git status` vs this file. Do not reset the mixed tree.
2. **Restart app**: `npm run dev` → `http://127.0.0.1:3000/` (prefer IPv4). If `reading 'call'` / missing `@phosphor-icons.js`: `rm -rf .next` then restart.
3. **Manual check**: Form↔Studio on `/dashboard/resume/[id]` — chrome order identical; studio Colors/Typography/Spacing below chrome; project name `**Apollo**` → bold Apollo, no asterisks; Design popup chips ~28px and preview full width.
4. **Optional `npm run build`** after `markdownInline.ts` (not re-run since that extract).
5. **Split commits** when the user asks — do not dump parser + studio + copy into one commit. Suggested slices: (a) OpenResume parser, (b) dual-mode studio, (c) copy/markdown/chrome.
6. **Untrack** `.codex/config.toml`; rotate PAT if it was ever pushed.
7. **Decide** `codex/new-resume-studio` (`5ea9530`) — merge leftover commits or delete after confirming the working tree has the needed bits.

## Decisions Made

- Dual-mode stays; Form is contain-scale, Studio is unscaled paper.
- Shared chrome component is `EditorChrome.tsx` (not a second `EditorTopChrome.tsx`).
- Markdown emphasis helpers are a **non-client** lib module.
- Product copy follows Teal / FlowCV / Kickresume (Master / Version, Job Tracker, Resumes, Job search, Interview prep). Keep OfferPath.
- Working tree left **uncommitted** on purpose (mixed sessions; no commit request).
- OfferPath dev server **stopped** for freeze (port 3000 free).

## Environment Notes

- **Dependencies installed**: none this session
- **Migrations run**: none
- **Env vars set**: none (uses existing `.env.local`)
- **Dev server command**: `npm run dev` on port **3000** (stopped at freeze)
- **Node/Python version**: Node `v25.6.0`
- **Other**: Next.js 15.5.15 (Webpack, not Turbopack). Production: `https://offerpath.cc.cd` from `main` `40257e6`. Vercel project `prj_EZQ6b6K1Ato7Pmb2qQQZUodJdpLl`. Supabase ref `ubkywtxwzrudstqvpafh`. GitHub: `https://github.com/alexjiaguo/offerpath.git`

## Known Issues / Blockers

- **Port 3000 is free** after freeze — Cursor browser will `ERR_CONNECTION_REFUSED` until `npm run dev`.
- **Poisoned `.next`**: HMR after a syntax error in `StylePanel.tsx` caused `undefined.call` + missing vendor chunk. Wipe `.next`.
- **`.codex/config.toml` still tracked** with a PAT despite gitignore.
- **Huge mixed uncommitted tree** — easy to lose if someone checks out/resets.
- Next.js warning: cross-origin `[::1]` → `/_next/*`; may need `allowedDevOrigins` if Simple Browser keeps failing while curl works.

## Agent Config Changes

- [ ] `AGENTS.md` (Codex, OpenCode)
- [ ] `CLAUDE.md` (Claude Code)
- [ ] `GEMINI.md` (Antigravity)
- [ ] `.cursorrules` or `.cursor/rules/` (Cursor)
- [ ] `.windsurfrules` (Windsurf)
- [ ] `.clinerules` (Cline)
- [ ] `.github/copilot-instructions.md` (GitHub Copilot)
- [ ] `.aider.conf.yml` or `CONVENTIONS.md` (Aider)
- [ ] `.goosehints` (Goose)
- [ ] `.roorules` or `.roo/rules/` (Roo Code)
- [ ] `.continue/config.json` (Continue)
- [ ] `.traerules` (Trae)
- [ ] `.zed/settings.json` (Zed)
- [x] None modified

**Summary of changes**: none this session. `AGENTS.md` typechange is from a **prior** session; not synced here.

## Scratch Files

- `.handoff/archive/HANDOFF-20260814.md`: previous Codex handoff — keep
- none other created for debug
